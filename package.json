{
  "name": "elemental-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "Elemental Agent Orchestration Platform - Monorepo",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "clean": "turbo run clean && rm -rf node_modules",
    "dev:platform": "turbo run dev --filter=@elemental/server --filter=@elemental/web"
  },
  "packageManager": "pnpm@8.15.5",
  "engines": {
    "node": ">=18.0.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/toolco/elemental.git"
  },
  "author": "Toolco",
  "license": "MIT",
  "devDependencies": {
    "bun-types": "^1.3.8",
    "turbo": "^2.0.0",
    "typescript": "^5.0.0"
  }
}
